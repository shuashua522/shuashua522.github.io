### 问题

1. codex读取文件时老有编码显示问题，如何解决？
2. codex安装依赖或者说用的python依赖是从哪来的？是否可以指定conda环境
3. 测试命令本身可运行，但当前环境对系统临时目录无权限，导致 tmp_path fixture 全部报错。我要改用本地 --basetemp 重跑，这能绕开环境限制并验证真实逻辑。；pytest 在该沙箱里会在会话清理阶段访问临时目录时报权限错误，无法得到稳定结果。我会改用一个本地 Python 校验脚本按同样场景逐项验证 base_env 行为和关键回归。
4. 让codex默认使用中文，除非需要，才使用其他语言
5. 我在 merge_audio.py 测试中发现一个稳定性问题：临时文件默认写到系统 Temp 目录会触发权限错误。我会改成在输出目录下生成临时清单文件并自动清理，然后重测回退场景。